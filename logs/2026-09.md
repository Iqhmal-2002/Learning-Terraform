Newest entry at the top. Add a new file each month (`logs/2026-09.md`).

Template for an entry — keep it to what future-me would want:

```
## YYYY-MM-DD — topic

**What I did:**

**What broke / what confused me:**

**What I'd do differently:**

**Links:**
```

---
## 2026-09-04 — Output and az login

**What I did:**
Understand what output block does.
Tried using az login instead of setting env values

**What broke / what confused me:**
Terraform hcl does not support code override like coding. You can only define a value once and that's it

**What I'd do differently:**

**Links:**

## 2026-09-03 — Variable

**What I did:**
Changed main.tf to use variable block, and then set variable when running terrafom apply

**What broke / what confused me:**
VS code has a hot exit where you can close it and files will reopen as if it has been saved. It was saved to backup and not actually in the original file, and so any terraform process do not see it. 

**What I'd do differently:**
Try using az login and not set env. Setting env is overkill for a tutorial, and really its a good to start with because it works universally but its not exactly used in production anyway. A good to know, don't use case. 
Also start saving all opened files with ctrl + k, release, S in VS code. Got bitten by just saved files today

**Links:**

## 2026-09-02 — refresh where I left off

**What I did:**
Pick up where I left off.

**What broke / what confused me:**
var. doesn't check for a file named var. There are not file hierarchy or structure in terraform, it loads everything at once and create a tree diagram. var. just looks up for a variable with the specified name, and then it loads the value of the variable

**What I'd do differently:**
Leave a clearer not for handover if I'm going on a long break. Makes things as easy to pick up once I'm back

**Links:**